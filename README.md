# Telegram бот для выдачи PDF-чек-листа

Бот проверяет подписку на канал @vyhod_est_law и автоматически выдаёт PDF-чек-лист "9 пунктов перед подписанием договора".

## Установка и запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Поместите PDF файл `Checklist_Dogovora_Yulia_Shnaptseva.pdf` в ту же папку, что и `bot.py`

3. (Опционально) Установите переменную окружения для токена:
```bash
export BOT_TOKEN=8534379995:AAFO-66CK3tOk6p-fC0FqjoFdIqm8xM759I
```
Или токен уже встроен в код.

4. Запустите бота:
```bash
python bot.py
```

## Настройка получения сообщений в личку

1. Запустите бота и напишите ему команду `/myid` в Telegram
2. Скопируйте ваш Telegram ID из ответа
3. Добавьте ваш ID в файл `bot.py` в переменную `ADMIN_IDS`:

```python
ADMIN_IDS = [123456789]  # Замените на ваш ID
```

Или установите через переменную окружения:
```bash
export ADMIN_IDS=123456789
```

После этого все сообщения от пользователей будут автоматически пересылаться вам в личку с кнопкой для ответа!

**Как ответить пользователю:**
1. Получите сообщение от пользователя в личку
2. Нажмите кнопку "💬 Ответить пользователю"
3. Напишите текст ответа - он автоматически отправится пользователю
4. Для отмены используйте команду `/cancel`

## Деплой на сервер для работы 24/7

Чтобы бот работал постоянно без вашего компьютера, разместите его на облачном хостинге.

📖 **Подробные инструкции:** См. файл [DEPLOY.md](DEPLOY.md)

**Быстрый вариант (Railway.app):**
1. Зарегистрируйтесь на https://railway.app
2. Создайте проект и подключите GitHub репозиторий
3. Добавьте переменные окружения `BOT_TOKEN` и `ADMIN_IDS`
4. Бот запустится автоматически и будет работать 24/7

## Важно

- Бот должен быть администратором канала @vyhod_est_law для проверки подписки
- Для постоянной работы на сервере используйте облачный хостинг (см. DEPLOY.md) или systemd сервис
- Чтобы получать сообщения в личку, настройте `ADMIN_IDS` (см. выше)
- Обязательно напишите боту `/start` хотя бы раз, чтобы бот мог отправлять вам сообщения

## Проблемы с Python 3.13

Если вы используете Python 3.13 и получаете ошибку `AttributeError: 'Updater' object has no attribute '_Updater__polling_cleanup_cb'`:
- В проекте есть файл `patch_telegram.py`, который автоматически исправляет эту проблему
- Патч применяется автоматически при запуске `bot.py`
- Альтернатива: используйте Python 3.12 или 3.11 для большей стабильности

## Ссылка на бота для поста

```
👉 [СТАРТ ЗДЕСЬ](https://t.me/ИМЯ_ВАШЕГО_БОТА?start=checklist)
```

Замените `ИМЯ_ВАШЕГО_БОТА` на username вашего бота.

